{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Todo List backend with nested todos, timeline calculation, and image uploads.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "npx prisma generate --schema=src/prisma/schema.prisma",
    "migrate": "npx prisma migrate deploy --schema=src/prisma/schema.prisma",
    "studio": "npx prisma studio --schema=src/prisma/schema.prisma",
    "reset": "npx prisma migrate reset --schema=src/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "@supabase/supabase-js": "^2.84.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.2",
    "pg": "^8.16.3",
    "prisma": "^6.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}